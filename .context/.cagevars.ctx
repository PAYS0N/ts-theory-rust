Configuration file that sets up environment variables for embedded development workflows. Extends `PATH` with Neovim, CMake, and ARM GNU Toolchain binaries, and defines SDK paths for Raspberry Pi Pico development (`pico-sdk` and `picotool`). Sourced by build scripts or shell initialization to make cross-compilation and embedded tools available without absolute paths.